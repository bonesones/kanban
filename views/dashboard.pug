extends layout.pug 

block title
    title #{title}
    

block content 
    header.project-header
        h1.project__title #{title}
        div.project-options
            div.project__users
                - for(let i = 0; i < 6; i++)
                    img(src="/images/user.png", class="project__user")
